parameters:
- name: version
  type: version
  default: 1.3.5

extends: [setuptools_package]

dependencies:
  build: [lockfile, lxml, python-daemon]
  run: [lockfile, lxml, python-daemon]

sources:
- url: http://ftp.mcs.anl.gov/pub/bcfg/bcfg2-{{version}}.tar.gz
  key:
    when version == 1.3.5: tar.gz:m6xs5ibzyfm2rdvcsec7ibmjwz32ba2z
