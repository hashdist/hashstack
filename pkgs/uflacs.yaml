parameters:
- name: version
  type: version
  default: 1.5.0

extends: [distutils_package]

dependencies:
  run: [ffc, numpy, six, ufl]

sources:
- url: https://bitbucket.org/fenics-project/uflacs/downloads/uflacs-{{version}}.tar.gz
  key:
    when version == 1.5.0: tar.gz:7asppyqwc774lcorerypjhybucqslgmy
