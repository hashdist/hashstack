parameters:
- name: version
  type: version
  default: 2.7.1

- name: relocatable
  type: bool
  default: false

extends: [autotools_package]

sources:
- url: http://ftp.heanet.ie/mirrors/gnu/patch/patch-{{version}}.tar.gz
  key:
    when version == 2.7.1: tar.gz:ybpsqzumgr2lyy5nzvekxlusduk6ohbf

when_build_dependency:
- prepend_path: PATH
  value: '${ARTIFACT}/bin'
