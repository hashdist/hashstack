parameters:
- name: version
  type: version
  default: 1.9.0.1

extends: [setuptools_package]

dependencies:
  build: [paramiko, jinja2, yaml]
  run: [paramiko, jinja2, yaml]

sources:
- key:
    when version == 1.9.0.1: tar.gz:wimt7wneaynlmjh2m4yrpfsbtj2z3ydl
  url: https://pypi.python.org/packages/source/a/ansible/ansible-{{version}}.tar.gz
