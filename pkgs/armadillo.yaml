parameters:
- name: version
  type: version
  default: 5.100.2

- name: relocatable
  type: bool
  default: false

extends: [cmake_package]

sources:
- key:
    when version == 5.100.2: tar.gz:mmkq6ev7pr6ftdvflnzhdwm7zy2j744g
  url: http://sourceforge.net/projects/arma/files/armadillo-{{version}}.tar.gz

build_stages:
  - name: setup_builddir
    after: prologue
    mode: replace
    handler: bash
    bash: |

# FIXME: why doesn't this work:
#  - name: configure
#    build_in_source: true
