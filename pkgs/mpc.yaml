parameters:
- name: version
  type: version
  default: 1.0.2

extends: [autotools_package]

dependencies:
  build: [gmp, mpfr]

sources:
- url: ftp://ftp.gnu.org/gnu/mpc/mpc-{{version}}.tar.gz
  key:
    when version == 1.0.2: tar.gz:wvq7ktmki6oo4o6ishxfe427dd7ym4jl
