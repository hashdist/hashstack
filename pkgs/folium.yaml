parameters:
- name: version
  type: version
  default: 0.1.2

extends: [distutils_package]

dependencies:
  run: [pandas, numpy]

sources:
- url: https://pypi.python.org/packages/source/f/folium/folium-{{version}}.tar.gz
  key:
    when version == 0.1.2: tar.gz:itakflkweus6v3dqrjztbjbu34dgkedh
