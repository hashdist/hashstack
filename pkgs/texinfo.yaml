extends: [autotools_package]

dependencies:
  build: [findutils, ncurses, gzip]

defaults:
  relocatable: false

sources:
- key: tar.gz:nogkgdu3n4etwvh6arbz4vkf4vsmmnuy
  url: http://ftp.heanet.ie/mirrors/gnu/texinfo/texinfo-5.2.tar.g
