parameters:
- name: version
  type: version
  default: 4.1

extends: [distutils_package]

dependencies:
  run: [backports-ssl_match_hostname]

sources:
- url: https://pypi.python.org/packages/source/t/tornado/tornado-{{version}}.tar.gz
  key:
    when version == 4.1: tar.gz:tgv5hlw6ixetoojun3ttqttraeqbehbx
