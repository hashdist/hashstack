parameters:
- name: version
  type: version
  default: 3.5.0

extends: [setuptools_package]

dependencies:
  build: [feedgenerator, jinja2, pygments, docutils, pytz, blinker, unidecode, six, python-dateutil]
  run: [feedgenerator, jinja2, pygments, docutils, pytz, blinker, unidecode, six, python-dateutil, MarkupSafe, python-markdown, typogrify]

sources:
- url: https://pypi.python.org/packages/source/p/pelican/pelican-{{version}}.tar.gz
  key:
    when version == 3.5.0: tar.gz:w7w2vchelc4z4ras4sselscofu4hhb7n
