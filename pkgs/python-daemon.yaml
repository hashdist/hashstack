parameters:
- name: version
  type: version
  default: 2.0.5

extends: [setuptools_package]

dependencies:
  build: [docutils]

sources:
- url: https://pypi.python.org/packages/source/p/python-daemon/python-daemon-{{version}}.tar.gz
  key:
    when version == 2.0.5: tar.gz:v7pe7jbt3fgqa4qg5yy2bfa5kw26wizk
