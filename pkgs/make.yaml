parameters:
- name: version
  type: version
  default: 4.0

- name: relocatable
  type: bool
  default: false

extends: [autotools_package]

sources:
- key:
    when version == 4.0: tar.bz2:4ydinr5p5xtczsginljqclhqqhverb62
  url: http://ftp.heanet.ie/mirrors/gnu/make/make-{{version}}.tar.bz2

when_build_dependency:
- prepend_path: PATH
  value: '${ARTIFACT}/bin'
