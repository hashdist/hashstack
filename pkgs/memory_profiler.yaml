extends: [setuptools_package]

dependencies:
  run:
    - when platform != 'Cygwin':
        - psutil

sources:
- key: tar.gz:isymgmavllf4uhqqtruwjvg2zpqlkhen
  url: https://github.com/fabianp/memory_profiler/archive/0.42.tar.gz
