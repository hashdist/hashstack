parameters:
- name: version
  type: version
  default: 1.10.1

extends: [distutils_package]

dependencies:
  run: [pycrypto]

sources:
- url: https://pypi.python.org/packages/source/p/paramiko/paramiko-{{version}}.tar.gz
  key:
    when version == 1.10.1: tar.gz:l6wa3oz5osml6st6doh2zeqmxclxvehc
