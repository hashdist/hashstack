parameters:
- name: version
  type: version
  default: 0.12.2

- name: relocatable
  type: bool
  default: false

extends: [autotools_package]

dependencies:
  build: [python]

sources:
- key:
    when version == 0.12.2: tar.gz:vr7hrlpjhzrt47wwfbjsxohgkdflude4
  url: http://nodejs.org/dist/v{{version}}/node-v{{version}}.tar.gz
