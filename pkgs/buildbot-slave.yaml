parameters:
- name: version
  type: version
  default: 0.8.10

extends: [distutils_package]

dependencies:
  build: [Twisted, zope-interface, setuptools]
  run: [Twisted, zope-interface]

sources:
- url: https://pypi.python.org/packages/source/b/buildbot-slave/buildbot-slave-{{version}}.zip
  key:
    when version == 0.8.10: zip:kwq5k3st37bhcksx4m5ulmlvdrb2jvrj
