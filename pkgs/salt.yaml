parameters:
- name: version
  type: version
  default: 2014.1.13

extends: [distutils_package]

dependencies:
  build: [jinja2, pycrypto, pyzmq, requests, yaml, apache-libcloud, MarkupSafe, msgpack-python, M2Crypto]
  run: [jinja2, pycrypto, pyzmq, requests, yaml, apache-libcloud, MarkupSafe, msgpack-python, M2Crypto]

sources:
- key:
    when version == 2014.1.13: tar.gz:bkzxi6hbrzsjb3go6tplt532edez77gg
  url: https://pypi.python.org/packages/source/s/salt/salt-{{version}}.tar.gz
