parameters:
- name: version
  type: version
  default: 1.2b3

extends: [setuptools_package]

dependencies:
  build: [docutils, jinja2, pygments]
  run: [jinja2, pygments]

sources:
  - url: https://pypi.python.org/packages/source/S/Sphinx/Sphinx-{{version}}.tar.gz
    key:
      when version == 1.2b3: tar.gz:tqibhpmfmqomdxmrsrhdvuwj2l6hrlgb
