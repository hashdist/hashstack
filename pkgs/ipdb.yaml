parameters:
- name: version
  type: version
  default: 0.8

extends: [setuptools_package]

dependencies:
  run: [ipython]

sources:
- url: https://pypi.python.org/packages/source/i/ipdb/ipdb-{{version}}.zip
  key:
    when version == 0.8: zip:bvfdh44omz5azahmdjna4rup4whfzxdb
