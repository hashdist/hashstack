parameters:
- name: version
  type: version
  default: 2.10

extends: [setuptools_package]

dependencies:
  build: [ply]

sources:
- url: https://pypi.python.org/packages/source/p/pycparser/pycparser-{{version}}.tar.gz
  key:
    when version == 2.10: tar.gz:sv6zrntbyc3ewwakw34uwes6bg3hcqku
