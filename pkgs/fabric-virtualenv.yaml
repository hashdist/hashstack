parameters:
- name: version
  type: version
  default: 0.2.1

extends: [setuptools_package]

dependencies:
  build: [fabric, paramiko]
  run: [fabric, paramiko]

sources:
- url: https://pypi.python.org/packages/source/f/fabric-virtualenv/fabric-virtualenv-{{version}}.tar.gz
  key:
    when version == 0.2.1: tar.gz:ieuohxfckg26ijsmrxwge6m3mxn2q62q
