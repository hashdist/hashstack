parameters:
- name: version
  type: version
  default: 4.6.5

# bin/GMXRC.zsh contains hard-coded path
- name: relocatable
  type: bool
  default: false

extends: [cmake_package]

dependencies:
  build: [fftw]

sources:
- url: ftp://ftp.gromacs.org/pub/gromacs/gromacs-{{version}}.tar.gz
  key:
    when version == 4.6.5: tar.gz:u4sc6mkzmoqrd2d72kdzk2lld2ayvslu
