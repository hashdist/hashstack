parameters:
- name: version
  type: version
  default: 0.9.2

extends: [setuptools_package]

dependencies:
  build: [pygments]

sources:
- url: https://pypi.python.org/packages/source/h/httpie/httpie-{{version}}.tar.gz
  key:
    when version == 0.9.2: tar.gz:v44yfvaxipmymyepd7toin6qje67ej4z
