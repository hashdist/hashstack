parameters:
- name: version
  type: version
  default: 1.11.1

extends: [setuptools_package]

dependencies:
  build: [gdal]

sources:
- key:
    when version == 1.11.1: tar.gz:zwdszwufyivezzpltv6j6t5vntntdbgb
  url: https://pypi.python.org/packages/source/G/GDAL/GDAL-{{version}}.tar.gz
