parameters:
- name: version
  type: version
  default: 1.3

extends: [setuptools_package]

sources:
- url: https://pypi.python.org/packages/source/b/blinker/blinker-{{version}}.tar.gz
  key:
    when version == 1.3: tar.gz:naiqccajeyrgdza2w64s6p3nepzvz6aw
