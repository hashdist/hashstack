parameters:
- name: version
  type: version
  default: 0.14

extends: [setuptools_package]

dependencies:
  build: [six, cryptography, openssl]

sources:
- url: https://pypi.python.org/packages/source/p/pyOpenSSL/pyOpenSSL-{{version}}.tar.gz
  key:
    when version == 0.14: tar.gz:vgo3rzm4ciatrlmkolxm5xgcjncrbuxo
