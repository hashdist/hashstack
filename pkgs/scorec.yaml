extends: [cmake_package]
dependencies:
  build: [mpi, parmetis, zoltan]

sources:
- url: http://www.scorec.rpi.edu/~chitak/Proteus/pumi.tar.gz
  key: tar.gz:c2jxcxqvzqkt2vkcynnbjmpgbptfyapy

build_stages:

- name: setup_builddir
  after: prologue
  handler: bash
  bash: |
    cd pumi
    mkdir -p _build
    cd _build

- name: configure
  extra: [
  '-DCMAKE_C_COMPILER=${MPICC}',
  '-DCMAKE_CXX_COMPILER=${MPICXX}',
  '-DMETIS_LIBRARY=${PARMETIS_DIR}/lib/libmetis.so',
  '-DPARMETIS_LIBRARY=${PARMETIS_DIR}/lib/libparmetis.so',
  '-DPARMETIS_INCLUDE_DIR=${PARMETIS_DIR}/include',
  '-DZOLTAN_LIBRARY=${ZOLTAN_DIR}/lib/libzoltan.a',
  '-DZOLTAN_INCLUDE_DIR=${ZOLTAN_DIR}/include',
  '-DBUILD_SHARED_LIBS=True'
  ]

