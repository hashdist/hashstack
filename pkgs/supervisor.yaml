parameters:
- name: version
  type: version
  default: 3.1.3

extends: [setuptools_package]

dependencies:
  build: [meld3, superlance]
  run: [meld3, superlance]

sources:
- url: http://pypi.python.org/packages/source/s/supervisor/supervisor-{{version}}.tar.gz
  key:
    when version == 3.1.3: tar.gz:4mwfi37i2ktoa6pmjam4jh6siu2nib22
