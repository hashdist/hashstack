parameters:
- name: version
  type: version
  default: 0.9

extends: [setuptools_package]

sources:
- url: https://pypi.python.org/packages/source/s/superlance/superlance-{{version}}.tar.gz
  key:
    when version == 0.9: tar.gz:f5mlz2ujvyhusiato6qmsllsbfxu6vmo
