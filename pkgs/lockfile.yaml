parameters:
- name: version
  type: version
  default: 0.10.2

extends: [setuptools_package]

dependencies:
  build: [pbr, pip]

sources:
- url: https://pypi.python.org/packages/source/l/lockfile/lockfile-{{version}}.tar.gz
  key:
    when version == 0.10.2: tar.gz:tzbcklyx2hoyt3rrorpayt56lcdcyjiu
