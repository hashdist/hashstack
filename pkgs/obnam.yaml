extends: [setuptools_package]

dependencies:
  build: [cliapp, larch, tracing, ttystatus, paramiko, pycrypto]

sources:
- key: tar.gz:64cgtqu6hulmocan762dhsacp2s7f64p
  url: http://code.liw.fi/debian/pool/main/o/obnam/obnam_1.8.orig.tar.gz

