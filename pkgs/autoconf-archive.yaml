parameters:
- name: version
  type: version
  default: 2014.02.28

extends: [autotools_package]

dependencies:
  build: [autoconf]
  run: [autoconf]

sources:
- url: http://ftp.heanet.ie/mirrors/gnu/autoconf-archive/autoconf-archive-{{version}}.tar.bz2
  key:
    when version == 2014.02.28: tar.bz2:vza5dtgp5slciig3ey7czooeb74emkxv
