parameters:
- name: version
  type: version
  default: 2.0.0

extends: [setuptools_package]

sources:
- url: https://pypi.python.org/packages/source/p/pyutil/pyutil-{{version}}.tar.gz
  key:
    when version == 2.0.0: tar.gz:bsuvzocyipc3bh33mw2323cc2gka4glg
