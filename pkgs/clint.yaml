parameters:
- name: version
  type: version
  default: 0.4.1

extends: [distutils_package]

sources:
- key:
    when version == 0.4.1: tar.gz:hkphxj6oviico26p5birbicggoatgrca
  url: https://pypi.python.org/packages/source/c/clint/clint-{{version}}.tar.gz
