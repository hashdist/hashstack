parameters:
- name: version
  type: version
  default: 1.5.0

extends: [distutils_package]

dependencies:
  run: [numpy, sympy]

sources:
- url: https://bitbucket.org/fenics-project/fiat/downloads/fiat-{{version}}.tar.gz
  key:
    when version == 1.5.0: tar.gz:fmct7kpxaom7reb3tb7zrjh2fmdn7fkz
