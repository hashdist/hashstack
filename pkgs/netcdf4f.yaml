parameters:
- name: version
  type: version
  default: 4.2

extends: [autotools_package]

dependencies:
  build: [mpi, hdf5, netcdf4]

sources:
- url: ftp://ftp.unidata.ucar.edu/pub/netcdf/netcdf-fortran-{{version}}.tar.gz
  key:
    when version == 4.2: tar.gz:a2pbud5vbfzpfayjeeeypjivmm3ju3o3
