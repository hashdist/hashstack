parameters:
- name: version
  type: version
  default: 1.5.0

extends: [distutils_package]

dependencies:
  run: [cmake, numpy, swig]

sources:
- url: https://bitbucket.org/fenics-project/instant/downloads/instant-{{version}}.tar.gz
  key:
    when version == 1.5.0: tar.gz:g3qefxrg3f7v4pkd6l2skrgzdoqpctq4
