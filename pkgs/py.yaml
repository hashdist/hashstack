parameters:
- name: version
  type: version
  default: 1.4.20

extends: [setuptools_package]

sources:
- url: https://pypi.python.org/packages/source/p/py/py-{{version}}.tar.gz
  key:
    when version == 1.4.20: tar.gz:epez3gplwkta5nychn2xpp6jrcwlacja
