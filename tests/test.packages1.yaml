# This builds the IPython, NumPy, SciPy, SymPy stack from scratch

extends:
- file: linux.yaml

packages:

  launcher:
  blas:
    use: host-blas
  python:
    host: false
    build_with: |
      bzip2, openssl
  ipython:
  nose:
  numpy:
  sphinx:
  sympy:
  scipy:
