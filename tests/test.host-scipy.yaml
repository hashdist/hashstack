# This test tests host installed Python, NumPy and SciPy

extends:
- file: suse.yaml

packages:

  launcher:

  python:
    host: true
    build_with: |

  numpy:
    use: host-numpy
  scipy:
    use: host-scipy
