# Tests PETSc and petsc4py

extends:
- file: linux.yaml

packages:

  launcher:
  blas:
    use: host-blas
  mpi:
    use: host-mpi
  python:
    host: false
    build_with: |
      bzip2, openssl
  petsc:
    build_with: |

    download: |

    coptflags: -O2
    link: shared
    debug: false
  petsc4py:
    with_conf: true
