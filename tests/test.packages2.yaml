# Tests some general configuration of packages

extends:
- file: debian.yaml

packages:

  launcher:

  python:
    host: false
    build_with: |
      bzip2, openssl

  mpi:
    use: host-mpi
  blas:
    use: host-blas

  nose:
  jinja2:
  szip:
  zlib:
  hdf5:
  fabric-virtualenv:
  sqlite:
  openssl:
