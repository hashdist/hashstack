# Tests some general configuration of packages

extends:
- file: suse.yaml

packages:

  launcher:

  python:
    host: false 

  mpi:
    use: host-mpi
  blas:
    use: host-blas

  nose:
  jinja2:
  distribute:
  petsc:
  petsc4py:
  szlib:
  zlib:

#  Currently fails (https://github.com/hashdist/hashstack2/issues/24):
#  hdf5:
