# Tests some general configuration of packages

extends:
- file: debian.yaml

packages:

  launcher:

  python:
  sqlite:
    use: host-sqlite

  mpi:
    use: host-mpi
  blas:
    use: host-blas

  nose:
  jinja2:
  szip:
  zlib:
  hdf5:
  fabric-virtualenv:
  sqlite:
  openssl:
