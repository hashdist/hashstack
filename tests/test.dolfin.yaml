extends:
- file: debian.yaml

packages:
  launcher:
  cmake:
    use: host-cmake
  python:
    host: true
    use_python_host_packages: true
  mpi:
    use: host-mpi
  blas:
    use: host-blas
  swig:
  boost:
    use: host-boost
  dolfin:
