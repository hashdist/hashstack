extends:
 - profile: common.yaml

parameters:
  platform: linux
  PATH: /usr/bin:/bin

packages:
  - zlib
  - szlib
  - python:
      use: host-python
  - numpy:
      use: host-numpy
  - nose
  - launcher
  - hdf5